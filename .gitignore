node_modules
.next
dist
.env
.env.*
!.env.example
*.log
coverage
.DS_Store
**/._*
service-account*.json
serviceAccount*.json
firebase-admin*.json
*-firebase-adminsdk-*.json

# Local Claude Code settings can accumulate tokens in allowed commands — keep untracked.
.claude/settings.local.json

# Generated build/model caches (some are still git-tracked; untrack with
# `git rm --cached <file>` in a dedicated commit if desired).
*.tsbuildinfo
.model-cache.json

# TypeScript incremental build cache — a build artifact, never source.
# It was tracked, so every local typecheck dirtied the tree and blocked deploys.
apps/frontend/tsconfig.tsbuildinfo
.last-deployed-commit
